// login auto close window is not guaranteed to work so having this page as a workaround (and this entire page
// is a workaround that will go away with password login flow)
const LoginCompleteView = () => {
  return <div>An email will be sent to allow you to login, this window should be closed.</div>;
};

export default LoginCompleteView;
