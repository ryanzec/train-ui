import Loading, { type LoadingProps } from '$/components/loading/loading';
import Section, { type LoadingSectionProps } from '$/components/loading/loading-section';

export type { LoadingProps, LoadingSectionProps };

export default Object.assign(Loading, { Section });
