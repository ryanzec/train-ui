import Dialog, { type DialogProps } from '$/components/dialog/dialog';

export { DialogFooterAlignment, dialogUtils } from '$/components/dialog/utils';

export type { DialogProps };

export default Dialog;
