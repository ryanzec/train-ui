import Droppable, { DroppableProps } from '$/components/drag-drop/droppable';
import Sortable, { SortableProps } from '$/components/drag-drop/sortable';

export type { DroppableProps, SortableProps };

export default Object.assign({}, { Sortable, Droppable });
