import Input, { InputProps } from '$/components/input/input';
import Icon, { InputIconProps } from '$/components/input/input-icon';

export type { InputProps, InputIconProps };

export default Object.assign(Input, { Icon });
